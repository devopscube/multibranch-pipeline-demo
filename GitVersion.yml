mode: Mainline
branches:
  master:
    regex: ^master$
    is-mainline: true
  develop:
    regex: ^develop$
    source-branches:
    - master
  feature:
    increment: Minor
    regex: ^feature?[/-]
    source-branches:
    - develop
ignore:
  sha: []
merge-message-formats: {}